dataDir: .

tags:
  - bvt-p (build_sku == 'cpu') or (build_sku == 'gpu')
  - nightly-p (build_sku == 'cpu') or (build_sku == 'gpu')
  - weekly-p (build_sku == 'cpu') or (build_sku == 'gpu')

testCases:
  Test cases pass:
    patterns:
      - "Test case"
      - "passed with"

  Test suites pass:
    patterns:
      - "Test suite"
      - "passed with"

