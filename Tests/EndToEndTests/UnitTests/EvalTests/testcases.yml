dataDir: .

tags:  
  - bvt-i (build_sku != 'gpu') and ((build_sku == 'cpu') or (device == 'gpu'))
  - nightly-i (build_sku != 'gpu') and ((build_sku == 'cpu') or (device == 'gpu'))
  - weekly-i (build_sku != 'gpu') and ((build_sku == 'cpu') or (device == 'gpu'))

testCases:
  Test cases pass:
    patterns:
      - "Test case"
      - "has passed with"

  Test suites pass:
    patterns:
      - "Test suite"
      - "has passed with"

  Test module passed:
    patterns:
      - "Test module"
      - "has passed with"
