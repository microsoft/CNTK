dataDir: .

tags:  
  # TODO move from l to separate leg, requires infra changes
  - bvt-l (build_sku != '1bitsgd') and ((build_sku == 'cpu') or (device == 'gpu'))
  - nightly-l (build_sku != '1bitsgd') and ((build_sku == 'cpu') or (device == 'gpu'))

testCases:
  Test cases pass:
    patterns:
      - "Test case"
      - "has passed with"

  Test suites pass:
    patterns:
      - "Test suite"
      - "has passed with"

  Test module passed:
    patterns:
      - "Test module"
      - "has passed with"
