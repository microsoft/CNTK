dataDir: .

tags:
  # CPU-only for now.
  - bvt-e (build_sku == 'cpu') and (os=='windows')
  - nightly-e (build_sku == 'cpu') and (os=='windows')

testCases:
  Each test passes:
    patterns:
      - Passed

  Test run successful:
    patterns:
      - "Test Run Successful."
