dataDir: ../../../Data
tags:
     # running targetting GPU on every BVT job in 'L' leg
     - bvt-l (build_sku == 'gpu')
     # running unconditionally on every Nightly job in 'L' leg
     - nightly-l (build_sku == 'gpu')

testCases:
  CNTK Run must be completed:
    patterns:
      - __COMPLETED__

  Must train epochs in exactly same order and parameters:
    patterns:
      - Starting Epoch {{integer}}
      - learning rate per sample = {{float}}
      - momentum = {{float}}

#  Epochs must be finished with expected results:
#    patterns:
#      - ^Finished Epoch[{{integer}} of {{integer}}]
#      - CrossEntropyWithSoftmax = {{float,tolerance=.1%}}
#      - EvalErrorPrediction = {{float,tolerance=.1%}}
#      - learningRatePerSample = {{float,tolerance=0.001%}}
#
#  Per-minibatch training results must match:
#    patterns:
#      - ^ Epoch[{{integer}} of {{integer}}]-Minibatch[{{integer}}-{{integer}}
#      - " * {{integer}}; "
#      - CrossEntropyWithSoftmax = {{float,tolerance=.1%}}
#      - EvalErrorPrediction = {{float,tolerance=.1%}}
#
