dataDir: .

tags:
    - bvt-e (build_sku == 'gpu') and (device=='gpu') and ((flavor == 'release') ^ (os == 'linux'))
    - nightly-e (build_sku == 'gpu') and (device=='gpu')
    - weekly-e (build_sku == 'gpu') and (device=='gpu')

testCases:
  Test run must be completed:
    patterns:
      - "CNTKv2Library-CifarResNet tests: Passed"

