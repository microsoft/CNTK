dataDir: .

tags:
    - bvt-e (((build_sku == '1bitsgd') and (device=='gpu')) or (build_sku == 'cpu')) and (flavor=='release')
    - nightly-e ((build_sku == '1bitsgd') or (build_sku == 'cpu'))

testCases:
  Test run must be completed:
    patterns:
      - "CNTKv2Library tests: Passed"

