dataDir: .

tags:
    - bvt-e ((build_sku == '1bitsgd') and (device=='gpu')) or (build_sku == 'cpu')
    - nightly-e ((build_sku == '1bitsgd') and (device=='gpu')) or (build_sku == 'cpu')

testCases:
  Test run must be completed:
    patterns:
      - "CNTKv2Library tests: Passed"

