dataDir: .

tags: 
  - bvt-l ((build_sku == 'gpu') or (build_sku == 'cpu')) and (os == 'windows') 
  - nightly-l ((build_sku == 'gpu') or (build_sku == 'cpu')) and (os == 'windows')

testCases:
  Test run must be completed:
    patterns:
      - "Evaluation Complete"
