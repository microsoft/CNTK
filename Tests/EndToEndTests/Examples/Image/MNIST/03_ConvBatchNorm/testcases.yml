dataDir: ../../../../Image/Data

tags:
    # running on every BVT job in 'P' (Parallel) leg
    #- bvt-p ((flavor=='debug') ^ (device=='cpu')) 
    # running every Nightly job in 'P' (Parallel) leg
    #- nightly-p

testCases:
  CNTK Run must be completed:
    patterns:
      - ^COMPLETED

